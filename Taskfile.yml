version: "3"

tasks:
  default:
    cmds:
      - task --list
    silent: true
  install:
    cmds:
      - uv sync
  run:
    cmds:
      - uv run main.py
  generate:
    cmds:
      - uv run generate.py
  preview:
    cmds:
      - open preview.html
